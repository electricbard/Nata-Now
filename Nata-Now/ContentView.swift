// ContentView.swift is unused — CompassView is the root view.
// This file is kept to avoid breaking any storyboard or preview references.

import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("Use CompassView instead")
    }
}
